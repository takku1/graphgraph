# Strategy Handoff — FAN-01 / OW-AC-09 (completed candidate)

## Target

- Contract: `docs/architecture/agent-interfaces/cli-transport/SYSTEM.md`
- Ticket: `FAN-01` / `OW-AC-09`

## Goal

Advertise the same machine-readable capability identity on CLI `status --json`
that MCP `project_status` already returns.

## Status

Implemented earlier this session: `advertised_capability()`. NEED_CHECKER.
Do not reuse this id for new work. New shell-lane work is `OW-SH-01`.
